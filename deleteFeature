    		// 도형 지우기, vector.getSource() 리턴값이 layer개체 임
    		//var feature = vector.getSource().getFeatures();
    		//var vType = feature[1].getGeometry().getType();
    		//vector.getSource().removeFeature(feature[1]);
    		/*
    		vector.getSource().forEachFeature( function(feature) {
    			var vType = feature.getGeometry().getType();
    			if (vType == "Circle"){
    				vector.getSource().removeFeature(feature);
    			}
    		});
    		*/

    		map.on('singleclick', function(evt) {
    		    var feature = map.forEachFeatureAtPixel(evt.pixel, function(feature, layer) {
    		        //you can add a condition on layer to restrict the listener
    		        return feature;
    		        });
    		    if (feature) {
    		    	vector.getSource().removeFeature(feature);
    		        //here you can add you code to display the coordinates or whatever you want to do
    		    }
    		});
